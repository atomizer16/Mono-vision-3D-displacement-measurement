# Depth Anything V2 inference configuration
model:
  name: depth-anything-v2
  weights: weights/depth_anything_v2.pth
inference:
  input_size: [518, 518]
  normalize:
    mean: [0.485, 0.456, 0.406]
    std: [0.229, 0.224, 0.225]
